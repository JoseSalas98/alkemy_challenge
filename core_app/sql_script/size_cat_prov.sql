CREATE TABLE IF NOT EXISTS "size_cat_prov" (
  "created_date"          DATE DEFAULT CURRENT_DATE,
  "categoria" 				    VARCHAR(140),
  "provincia" 		        VARCHAR(140),
  "size"                  INTEGER
);